// config.js

const API_BASE_URL = "http://localhost/mywellbeing/api"; 
// 👆 Replace with your backend folder path or deployed server URL

const API_CONFIG = {
  REGISTER: `${API_BASE_URL}/register.php`,
  LOGIN: `${API_BASE_URL}/login.php`,
};

export default API_CONFIG;
